{{ full_product.title }}
  
{%- assign downcase_title = product.title | downcase -%}
{% if downcase_title != 'default title' %}
  <br />
  {{ product.title | replace: ' / ', '<br />' }}
{% endif %}
  