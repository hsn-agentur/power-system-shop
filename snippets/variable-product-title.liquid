{{ full_product.title }}
  
{%- assign downcase_title = variant.title | downcase -%}
{% if downcase_title != 'default title' %}
  <br />
  {{ variant.title | replace: ' / ', '<br />' }}
{% endif %}
  